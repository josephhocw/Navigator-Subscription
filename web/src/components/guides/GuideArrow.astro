---
// A downward arrow divider between two stacked screenshots: "do this" (above)
// → "here's the result" (below). Cyan accent to match the GuideFlow arrows.
// Decorative only, so it's hidden from screen readers.
---
<div class="garrow" aria-hidden="true">
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
    <path d="M12 4v14"></path>
    <path d="m6 12 6 6 6-6"></path>
  </svg>
</div>
